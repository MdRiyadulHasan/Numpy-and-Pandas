import numpy as np
zero_matrix=np.zeros((3,3))
print(zero_matrix)
one_matrix = np.ones((4,3))
print(one_matrix)
full_matrix = np.full((2,2), 9)
print(full_matrix)
identity_matrix = np.eye(3)
print(identity_matrix)
